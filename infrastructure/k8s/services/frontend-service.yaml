apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: search-service
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: metrics
      port: 2112
      targetPort: 2112
