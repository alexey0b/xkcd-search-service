apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: search-service
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 2113
      targetPort: 2113
